/**
 * SONARIS — Firebase Configuration
 * TODO: Replace with your Firebase project configuration
 */

// eslint-disable-next-line no-unused-vars
const FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY_HERE",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};

/*
 * SETUP INSTRUCTIONS:
 * 1. Go to https://console.firebase.google.com/
 * 2. Create a new project (or use an existing one)
 * 3. Go to Project Settings > General > Your Apps > Web App
 * 4. Register a web app and copy the config object
 * 5. Replace the placeholder values above with your config
 * 6. Enable Authentication > Google Sign-In in the Firebase Console
 * 7. Enable Firestore Database in the Firebase Console
 */
